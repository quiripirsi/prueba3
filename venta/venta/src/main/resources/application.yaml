server:
  port: ${PORT:8082}

spring:
  application:
    name: ms-ventas

  datasource:
    url: jdbc:mysql://localhost:3306/venta_db
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true