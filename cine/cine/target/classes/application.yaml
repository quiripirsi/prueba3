server:
  port: ${PORT:8081}

spring:
  application:
    name: ms-cine

  datasource:
    url: jdbc:mysql://localhost:3306/cine_db
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true